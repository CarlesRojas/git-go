# Change Log

## [Unreleased]

- Hide the undo button and its shortcut when the last action is already pushed, so an undo can never rewrite published history. Undoing a pull is exempt, since it only leaves the branch behind its upstream

## [1.0.0]

- Release
